# application.yml
# (opzionale) posizione alternativa se non localhost:8888


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: Mysql@123

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
boot:
  message: "Hello, Employee Service Spring Cloud Bus Demo: remote application.yml"

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
